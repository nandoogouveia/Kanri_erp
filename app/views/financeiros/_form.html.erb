<%= form_with(model: financeiro, local: true) do |form| %>
  <% if financeiro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(financeiro.errors.count, "error") %> prohibited this financeiro from being saved:</h2>

      <ul>
      <% financeiro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :financial %>
    <%= form.text_field :financial %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
